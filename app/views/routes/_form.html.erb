<%= form_with(model: @route, local: true) do |form| %>
  
  <%= render 'shared/errors', resource: @route %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
