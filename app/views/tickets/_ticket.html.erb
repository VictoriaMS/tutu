<tr>
  <td><%= ticket.number %></td>
  <td><%= "#{ticket.first_station.title} - #{ticket.last_station.title}" %></td>
  <td><%= link_to t('common.show'), ticket_path(ticket) %></td>
  <td><%= link_to t('common.destroy'), ticket, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
